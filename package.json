{
  "name": "crea-arica-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-note.mjs",
    "check:ascii": "node scripts/check-ascii-paths.mjs",
    "lint": "npm run check:ascii && eslint src --ext .js",
    "test": "node --test",
    "build": "node scripts/build.mjs"
  }
}
